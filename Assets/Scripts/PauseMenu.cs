using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PauseMenu : MonoBehaviour
{
    //public float Opacity = 0.4f;
    public GameObject Panel;

    private void Start()
    {
        //var image = GetComponent<Image>();
        //LeanTween.value(gameObject, a => image.color = new Color(0, 0, 0, a), 0.4f, 0, 0.5f).setEaseInOutExpo().setLoopPingPong();

        //var rectTrans = Panel.GetComponent<RectTransform>();
        //LeanTween.moveX(rectTrans, -rectTrans.rect.width, 0.5f).setEaseInOutExpo().setLoopPingPong();
    }
}
